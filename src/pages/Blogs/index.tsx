import ErrorBanner from "@/components/ui/ErrorBanner";
import Layout from "@/layout";

const Blogs = () => {
      return (
            <Layout>
                  <ErrorBanner />
            </Layout>
      );
};

export default Blogs;
