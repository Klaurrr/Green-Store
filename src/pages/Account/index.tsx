import AccountPage from "@/components/screens/Account";
import Layout from "@/layout";

const Account = () => {
      return (
            <Layout>
                  <AccountPage />
            </Layout>
      );
};

export default Account;
