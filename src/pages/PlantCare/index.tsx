import ErrorBanner from "@/components/ui/ErrorBanner";
import Layout from "@/layout";

const PlantCare = () => {
      return (
            <Layout>
                  <ErrorBanner />
            </Layout>
      );
};

export default PlantCare;
