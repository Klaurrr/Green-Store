import ErrorBanner from "@/components/ui/ErrorBanner";
import Layout from "@/layout";

const Wishlist = () => {
      return (
            <Layout>
                  <ErrorBanner />
            </Layout>
      );
};

export default Wishlist;
