import ShopPage from "@/components/screens/Shop";
import Layout from "@/layout";

const Shop = () => {
      return (
            <Layout>
                  <ShopPage />
            </Layout>
      );
};

export default Shop;
