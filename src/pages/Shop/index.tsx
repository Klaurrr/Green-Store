import ErrorBanner from "@/components/ui/ErrorBanner";
import Layout from "@/layout";

const Shop = () => {
      return (
            <Layout>
                  <ErrorBanner />
            </Layout>
      );
};

export default Shop;
